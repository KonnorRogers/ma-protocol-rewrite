import Note from "../../../components/note/index"

export default function StrokeNote(_props) {
  return (
    <Note id="stroke-note">
      This checklist is included as a resource for EMTs and receiving hospitals.
      If used, please leave a copy with the patient and document all elements on
      Patient Care Report
    </Note>
  )
}
